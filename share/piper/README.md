# Piper voices (offline)

`scripts/setup_tts.sh` downloads the binary + Jarvis/Friday models here.

| SSoT `tts.voice` | Model | Character |
|---|---|---|
| `jarvis` | `voices/en_GB-alan-medium.onnx` | British male |
| `friday` | `voices/en_GB-alba-medium.onnx` | British / Scottish female |

Not the film actors — closest offline neural match. Runtime needs no network.
